# 0x03. SQL and NoSQL Injection - Basic Discovery

This project focuses on identifying and understanding SQL Injection (SQLi) vulnerabilities within web applications. The goal of this specific task was to discover which parameter in the target application is susceptible to SQL injection attacks.

## Environment Setup

1. **Target Machine:** `cyber_websec_0x01`
2. **Local Domain Mapping:** Added the active instance IP to `/etc/hosts`:
   ```text
   10.42.76.70 web0x01.hbtn
